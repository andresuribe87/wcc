<h1>Editing user</h1>
<p>
<strong>Email</strong>: <%= @user.email %>
</p>
<%= @current_user.is_admin ? (render 'admin_form') : (render 'form') %>

<%= link_to 'Show', @user %> |
<%= link_to 'Back', users_path %>
